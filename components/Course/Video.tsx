import styles from "@/styles/Course/Video.module.scss";

const Video = () => {
  return (
    <div className={styles.video}>
      <i className="bx bx-right-arrow" />
    </div>
  );
};

export default Video;
